rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Basic rules for testing/development - allows authenticated users to read/write.
    // In a real prod environment, these would be locked down by role.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
